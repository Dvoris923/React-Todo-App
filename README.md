# 📋 React Todo App

A simple and interactive React Todo application built using **Vite**, **React** and **TypeScript**.

🔗 [**Live Demo:**](https://dvoris923.github.io/React-Todo-App/)


## Installation and Setup
To install the project and run it locally, follow these steps:

1.  **Clone the repository**:
    ```bash
    git clone cd https://github.com/username/React-Todo-App.git
    ```
2.  **Navigate to the project directory**:
    ```bash
    cd React-Todo-App
    ```
3.  **Install dependencies**:
    ```bash
    npm install
    ```
4.  **Start the local development server**:
    ```bash
    npm run start
    ```

---

## 📌 About The Project

This is a modern Todo List application created with React and TypeScript.
It allows users to manage tasks — add new tasks, mark them as complete, and delete them.

The project is built using **Vite** for fast development and bundling, and **React** for UI rendering.

---

## 🚀 Features

- Add new Todo tasks
- Mark tasks as completed
- Delete tasks
- (Optional) Task filtering (All / Active / Completed)
- Interactive UI with component-based structure

---

## 🛠 Technologies Used

This project uses the following:

- **Vite** – fast build tool and development server for React
- **React** – UI library for building the Todo interface
- **TypeScript** – typed superset of JavaScript for safer code
- **HTML & CSS** – structure and styling of the app

---

## 🎮 How to Use

1. In the input field, type a new task and press **Enter** or click the Add button.
2. Your task appears in the list below.
3. Click a task to mark it as completed or use the delete button to remove it.

---

